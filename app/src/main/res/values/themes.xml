<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ثيم التطبيق بدون ActionBar ومع شريط حالة شفاف -->
    <style name="Theme.Smm.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- إلغاء العنوان والاكشن بار الافتراضيين -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <!-- شريط الحالة شفاف (هام: استخدم android:statusBarColor) -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- تنسيقات اختيارية -->
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowContentTransitions">true</item>

        <!-- لو احتجت إخفاء عنوان النظام نهائيًا -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>
